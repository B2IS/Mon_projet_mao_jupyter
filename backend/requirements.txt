# ─────────────────────────────────────────────────────────────────────────────
# SIGEP-DPE Backend — Python Dependencies
# Architecture ML : FastAPI + Supabase + IA Open Source
# ─────────────────────────────────────────────────────────────────────────────

# ── API & Serveur ──
fastapi>=0.110.0
uvicorn[standard]>=0.29.0
python-multipart>=0.0.9

# ── Base de données ──
supabase>=2.4.0
psycopg2-binary>=2.9.9
sqlalchemy>=2.0.0
alembic>=1.13.0

# ── Data Science & ML ──
numpy>=1.26.0
pandas>=2.2.0
scikit-learn>=1.4.0

# ── IA Open Source ──
transformers>=4.39.0
torch>=2.2.0
sentence-transformers>=2.6.0
accelerate>=0.28.0

# ── Swarm multi-agents (Migration IA) ──
langgraph>=0.2.0
langchain-core>=0.3.0
mcp>=1.0.0                 # Model Context Protocol (exposition des outils swarm)

# ── Extraction documentaire multi-format ──
pdfplumber>=0.11.0         # texte PDF natif
pypdf>=4.2.0               # fallback PDF
python-docx>=1.1.0         # Word (.docx)
openpyxl>=3.1.0            # Excel (.xlsx)
python-pptx>=0.6.23        # PowerPoint (.pptx)
ezdxf>=1.2.0               # AutoCAD (.dxf ; .dwg via ODA File Converter)
rarfile>=4.2               # archives RAR (nécessite binaire 'unrar')
py7zr>=0.21.0              # archives 7z
pdf2image>=1.17.0          # rasterisation PDF scannés → OCR
Pillow>=10.2.0             # images

# ── OCR Open Source (meilleur disponible : docTR > EasyOCR > Tesseract) ──
python-doctr[torch]>=0.8.0 # docTR (Mindee) — OCR deep learning
easyocr>=1.7.0             # alternative robuste multi-langue
pytesseract>=0.3.10        # binding Tesseract (nécessite binaire 'tesseract')

# ── Outils & Utilitaires ──
python-dotenv>=1.0.0
pydantic>=2.6.0
pydantic-settings>=2.2.0
python-jose[cryptography]>=3.3.0
passlib[bcrypt]>=1.7.4
httpx>=0.27.0

# ── Tests ──
pytest>=8.1.0
pytest-asyncio>=0.23.0
pytest-cov>=4.1.0

# ── Logging & Monitoring ──
structlog>=24.1.0
prometheus-client>=0.20.0

# ── Déploiement ──
gunicorn>=21.2.0
