# ─────────────────────────────────────────────────────────────────────────────
# SIGEPP-DPE — Django Backend
# Stack : Django 5 + DRF + JWT + Supabase/PostgreSQL
# ─────────────────────────────────────────────────────────────────────────────

# ── Framework ──
django>=5.0,<6.0
djangorestframework>=3.15
djangorestframework-simplejwt>=5.3
django-cors-headers>=4.3
django-filter>=24.0

# ── Base de données ──
psycopg2-binary>=2.9.9

# ── Utilitaires ──
python-decouple>=3.8       # variables d'environnement (.env)
django-extensions>=3.2
Pillow>=10.3               # gestion images/uploads

# ── API & Docs ──
drf-spectacular>=0.27      # OpenAPI / Swagger auto-généré

# ── Outils dev ──
django-debug-toolbar>=4.3
