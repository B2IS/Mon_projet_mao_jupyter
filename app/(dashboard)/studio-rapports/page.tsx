import StudioRapports from '@/components/dashboard/StudioRapports';
export default function StudioRapportsPage() {
  return (
    <main style={{ flex: 1, display: 'flex', flexDirection: 'column', overflowY: 'auto', minHeight: 0 }}>
      <StudioRapports />
    </main>
  );
}
